<UserControl x:Class="QualityControlSystem.WPF.Views.FrameCardsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Command="{Binding AddFrameCommand}" Content="Добавить" Width="80" Margin="5"/>
            <Button Command="{Binding EditFrameCommand}" Content="Редактировать" Width="100" Margin="5"/>
            <Button Command="{Binding DeleteFrameCommand}" Content="Удалить" Width="80" Margin="5"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5,0,0,10">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Width="260"
                     Margin="0,0,10,0"
                     ToolTip="Поиск"/>
            <ComboBox ItemsSource="{Binding MaterialOptions}"
                      SelectedItem="{Binding SelectedMaterialFilter}"
                      DisplayMemberPath="Name"
                      Width="180"
                      Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding WorkshopOptions}"
                      SelectedItem="{Binding SelectedWorkshopFilter}"
                      DisplayMemberPath="Name"
                      Width="180"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding FramesView}"
                  SelectedItem="{Binding SelectedFrame}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  RowHeight="72">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTemplateColumn Header="Изображение" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Width="88"
                                    Height="56"
                                    Background="#EEEEEE"
                                    BorderBrush="#CCCCCC"
                                    BorderThickness="1">
                                <Image Source="{Binding ImagePreview}" Stretch="Uniform"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Материал" Binding="{Binding MaterialName}" Width="140"/>
                <DataGridTextColumn Header="Цех" Binding="{Binding WorkshopName}" Width="160"/>
                <DataGridTextColumn Header="Вес" Binding="{Binding Weight}" Width="80"/>
                <DataGridTextColumn Header="Длина" Binding="{Binding Length}" Width="80"/>
                <DataGridTextColumn Header="Ширина" Binding="{Binding Width}" Width="80"/>
                <DataGridTextColumn Header="Высота" Binding="{Binding Height}" Width="80"/>
                <DataGridTextColumn Header="Путь к изображению" Binding="{Binding ImagePath}" Width="220"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Margin="5" Foreground="Red"/>
    </Grid>
</UserControl>
