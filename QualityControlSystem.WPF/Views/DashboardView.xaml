<UserControl x:Class="QualityControlSystem.WPF.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#F5F5F5">
        <StackPanel Margin="20">
            <TextBlock Text="Панель управления" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>
            <TextBlock Text="{Binding WelcomeMessage}" FontSize="16"/>
            <Button Content="Выйти" Command="{Binding LogoutCommand}" HorizontalAlignment="Left" Margin="0,20,0,0" Padding="10,5"/>
        </StackPanel>
    </Grid>
</UserControl>