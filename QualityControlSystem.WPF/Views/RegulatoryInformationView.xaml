<UserControl x:Class="QualityControlSystem.WPF.Views.RegulatoryInformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Нормативно-справочная информация"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,14"/>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Height="30"
                     Margin="0,0,8,0"
                     VerticalContentAlignment="Center"/>

            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding TypeFilters}"
                      DisplayMemberPath="DisplayName"
                      SelectedValuePath="Value"
                      SelectedValue="{Binding SelectedType}"
                      Height="30"
                      Margin="0,0,8,0"/>

            <Button Grid.Column="2"
                    Content="Поиск"
                    Command="{Binding SearchCommand}"
                    Width="80"
                    Height="30"
                    Margin="0,0,12,0"/>

            <Button Grid.Column="4"
                    Content="Добавить"
                    Command="{Binding AddCommand}"
                    Width="90"
                    Height="30"
                    Margin="0,0,8,0"/>

            <Button Grid.Column="5"
                    Content="Редактировать"
                    Command="{Binding EditCommand}"
                    Width="120"
                    Height="30"
                    Margin="0,0,8,0"/>

            <Button Grid.Column="6"
                    Content="Удалить"
                    Command="{Binding DeleteCommand}"
                    Width="90"
                    Height="30"/>
        </Grid>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding RegulatoryItems}"
                  SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding RegulatoryInformationId}" Width="60"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding TypeDisplay}" Width="120"/>
                <DataGridTextColumn Header="Мин." Binding="{Binding MinValue}" Width="90"/>
                <DataGridTextColumn Header="Макс." Binding="{Binding MaxValue}" Width="90"/>
                <DataGridTextColumn Header="Ед. изм." Binding="{Binding MeasurementDisplay}" Width="100"/>
                <DataGridTextColumn Header="Дата начала" Binding="{Binding StartDate, StringFormat={}{0:dd.MM.yyyy}}" Width="120"/>
                <DataGridTextColumn Header="Дата окончания" Binding="{Binding EndDate, StringFormat={}{0:dd.MM.yyyy}}" Width="130"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3"
                   Text="{Binding StatusMessage}"
                   Margin="4,10,0,0"
                   Foreground="#B00020"/>
    </Grid>
</UserControl>
